# 本轮重构任务总览（并行）
**时间戳**: 2026-04-18 21:10:27

## 目标
1. 修复登录主链路阻塞（模拟扫码成功后不跳转）
2. 收敛重构后的边界与质量门禁
3. 打通下移（预发/NAS）所需最小部署闭环

## 风险清单（按优先级）
### P0（阻塞）
- 前端弹窗测量逻辑导致递归/重入，触发 `RangeError: Maximum call stack size exceeded`，影响登录成功后的跳转链路。

### P1（高）
- 仓库脚本引用 `ops/**`，但目录物料缺失，部署链路不可复现。
- 登录后路由守卫与 `reLaunch` 时序存在误拦截风险（需回归确认）。

### P2（中）
- Dockerfile 注释与基础镜像版本表述不一致，存在认知偏差风险。

## 并行任务分工
- FE（组件）：修复 AppModal 测量重入问题并回归登录跳转。
- FA（前端架构）：评审并沉淀 SelectorQuery 使用规范。
- QA：执行主链路回归 + 稳定性重复验证。
- BE：确认模拟登录接口稳定，跟踪 debug/log 错误消失。
- OPS：补齐下移部署物料与 smoke 验证脚本。

## 统一验收口径
- 不再出现 RangeError 栈溢出前端日志。
- 模拟扫码成功后 1.5s 内稳定跳转目标页。
- FE lint/type-check/build 通过；BE lint/build/test 通过。
- 预发环境可复现部署，`/health`、`/api`、短链跳转可用。
