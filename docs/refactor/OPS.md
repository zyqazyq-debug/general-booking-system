# OPS任务
**时间戳**: 2026-04-18 21:10:27

## 本轮目标
补齐下移阶段最小部署闭环，确保可复现与可回滚。

## 工作项
1. 梳理并补齐 `ops/**` 缺失物料（部署入口、smoke、历史记录）。
2. 明确预发/NAS 拓扑：
   - 前端静态；
   - `/api` 反向代理；
   - 后端容器与数据库连接。
3. 输出最小可执行部署步骤（安装、启动、验证、回滚）。
4. 增加 smoke 检查：
   - `/health`；
   - 登录接口；
   - 短链跳转。

## 验收标准
- 新环境可按文档完整部署一次（无人工口头补充）。
- smoke 检查可执行并有结果记录。
- 回滚路径可演练。

## 输出
- 在 `docs/feedback/OPS.md`（若无则按团队简称）覆盖写入本轮总结（带时间戳）。
