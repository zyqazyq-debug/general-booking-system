# 验收清单 (Checklist)

## 1. 信用点机制验证
- [ ] **冻结逻辑**：用户下单后，`credit_frozen` 增加，`credit_balance` 减少，总资产不变。
- [ ] **解冻逻辑**：订单状态变为 COMPLETED 后，`credit_frozen` 减少，`credit_balance` 增加（原路返回）。
- [ ] **违约扣除**：订单状态变为 NO_SHOW 后，`credit_frozen` 减少，`credit_balance` 不变（被扣除）。
- [ ] **余额不足拦截**：当 `credit_balance` < 日程设定的 `deposit_points` 时，无法下单。

## 2. 分销与价格同步
- [ ] **动态计算**：发布者将价格从 100 改为 200，中介设置的是 "+20%"。消费者刷新页面应看到 240 (200 * 1.2)。
- [ ] **隐私隔离**：通过中介链接进入的消费者，无法在页面 Response 中找到发布者的手机号或原始 ID。
- [ ] **整包代理**：中介创建一次代理链接，即可代理该发布者未来所有的开放日程。

## 3. 日程与库存
- [ ] **时区正确性**：发布者设置 "2023-10-01 10:00 (UTC+8)"，消费者在 UTC+0 时区应看到 "02:00"。
- [ ] **并发冲突**：两个消费者同时点击同一时段预约，只能有一个成功，另一个提示“已被抢占”。
- [ ] **循环规则**：设置“每周一”开放，查看日历时，下周一、下下周一均应显示为“可预约”。

## 4. 业务规则
- [ ] **单方裁决权**：发布者点击“完成”或“爽约”无需消费者确认，状态立即更新。
- [ ] **不可提现**：系统中无提现入口，信用点仅在系统内流转。
