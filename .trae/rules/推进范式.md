开始工作前必须读取docs/feedback下的所有人的当前进度文档，了解同事进度。

开始工作前必须读取docs/refactor下自己负责部分的新一轮任务文档，明确自己本轮要完成的工作。

工作结束后必须追加写入docs/feedback文件夹文档，以自己身份简称为文档名，清除原来内容，写入这一轮总结，带上时间戳。

项目经理每次发布任务时都写入docs/refactor文件夹，按不同人员任务写入，写入的必须是这一轮可以并行进行的任务，内容带上时间戳，覆盖源文件
